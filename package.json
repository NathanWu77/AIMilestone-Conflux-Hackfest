{
  "name": "conflux-milestone-escrow",
  "version": "1.0.0",
  "description": "Gas-sponsored milestone escrow AI agent for Conflux eSpace",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js --network confluxTestnet",
    "setup-sponsorship": "hardhat run scripts/setup-sponsorship.js --network confluxTestnet",
    "demo": "hardhat run scripts/demo-flow.js --network confluxTestnet"
  },
  "keywords": [
    "conflux",
    "escrow",
    "milestone",
    "ai-agent",
    "gas-sponsorship"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "js-conflux-sdk": "^2.5.0",
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.3.1"
  }
}